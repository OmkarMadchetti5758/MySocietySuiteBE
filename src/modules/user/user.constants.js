"use strict";

const USER_ERRORS = Object.freeze({
    USER_NOT_FOUND: "User not found.",
    EMAIL_EXISTS: "User with this email already exists.",
    MOBILE_EXISTS: "User with this mobile number already exists.",
});

module.exports = {
    USER_ERRORS,
};
